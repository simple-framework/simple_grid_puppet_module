{
  "description": "Implements Docker Swarm from the SIMPLE Framework",
  
  "implementations": [
    {"name": "swarm.rb"}
  ],
  "parameters": {
    "augmented_site_level_config_file": {
      "type": "Optional[String[1]]",
      "description": "The location of the augmented site level configuration file generated by Simple Grid YAML Compiler"
    },
    "modulepath": {
      "type": "String[1]",
      "description": "modulepath to use when running docker swarm related tasks"
    }
  }
}
